RSpec.describe Footable::Legacy::Rails do
  it "has a version number" do
    expect(Footable::Legacy::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
